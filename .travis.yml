sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t xg/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run xg/react-test npm test -- --coverage

after_success:
  - docker build -t xg/multi-client ./client
  - docker build -t xg/multi-nginx ./nginx
  - docker build -t xg/multi-server ./server
  - docker build -t xg/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push xg/multi-client
  - docker push xg/multi-nginx
  - docker push xg/multi-server
  - docker push xg/multi-worker


